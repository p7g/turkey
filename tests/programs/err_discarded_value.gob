-- A value computed and thrown away is an error unless there is no value
-- (SPEC-DELTAS 66). `let _ = ...` says the discard is meant.
fun main() {
    var xs = [1, 2, 3]
    let _ = Array.pop(xs)
    Array.pop(xs)
    print(Int.toString(len(xs)))
}
