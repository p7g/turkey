-- The type an existential hides may not leave the pattern that opened it
-- (SPEC-DELTAS 68). It stands for whatever type was packed, so nothing outside
-- the arm can be equal to it.

type Some = Some[Show a](a)

fun leak(Some(x)) = x

fun main() {
    print(show(leak(Some(1))))
}
