-- A `let` has no body for a hidden type to live in, so it may not open one
-- (SPEC-DELTAS 68). GHC has the same rule.

type Some = Some[Show a](a)

fun main() {
    let Some(x) = Some(1)
    print(show(x))
}
