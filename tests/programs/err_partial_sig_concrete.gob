-- A header variable is any type, even in a partial annotation (SPEC-DELTAS
-- 67). Delta 38's closing example: the body pins `a` to a number.
fun five(x) -> a { 5 }

fun main() {
    print(Int.toString(five(0)))
}
