-- A partial annotation's context is the whole of what it may assume about its
-- variables (SPEC-DELTAS 67), as a complete one's is (delta 38).
fun describe(x : a, prefix) -> String = prefix + show(x)

fun main() {
    print(describe(1, "n = "))
}
