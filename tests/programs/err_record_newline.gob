-- A newline between fields is a separator (SPEC-DELTAS 64), so `-b` on its
-- own line is a new field rather than `a - b`, and that is not a field.
type P = P { x : Int, y : Int }

fun main() {
    let a = 1
    let b = 2
    let p = P { x = a
        -b }
    print(Int.toString(p.x))
}
