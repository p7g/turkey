-- A `let` takes an irrefutable pattern since SPEC-DELTAS 63; a refutable one
-- used to lower to a match that panicked. `if let` is the form for this.
fun main() {
    let Some(x) = Array.pop([1])
    print(Int.toString(x))
}
