fun main() {
    let a = Array.new(4)
    -- `Array.new` reserves capacity but does not create initialized elements.
    -- Once one element has been pushed, the next slot remains out of bounds.
    Array.push(a, 0)
    print(Int.toString(a[1]))
}
